apiVersion: v1
kind: Service
metadata:
  name: inference-router-svc
  namespace: llm-serving
  labels:
    app: inference-router
spec:
  selector:
    app: inference-router
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: LoadBalancer
